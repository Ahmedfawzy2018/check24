{% extends 'admin/base.html.twig' %}

{% block title %}IpBlock{% endblock %}

{% block body %}
    <h1>IpBlock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ip_block.id }}</td>
            </tr>
            <tr>
                <th>Ip</th>
                <td>{{ ip_block.ip }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_ip_block_index') }}">back to list</a>

    <a href="{{ path('admin_ip_block_edit', {'id': ip_block.id}) }}">edit</a>

    {{ include('admin/ip_block/_delete_form.html.twig') }}
{% endblock %}
